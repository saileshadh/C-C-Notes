C++ Array Declaration;
dataType arrayName[arraySize];

simple example code:
Input array element and display it:
###############################
#include <iostream>
using namespace std;

int main() {
int i;
  int numbers[5];
    for (i=0;i<5;i++)
    {
        cin>>numbers[i];
    }
    for(i=0;i>5;i++){
        cout<<numbers[i];
    }

  return 0;
}
